{
  "scripts": {
    "start": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm ci && npm start",
    "client": "cd client && npm ci && npm start"
  },
  "devDependencies": {
    "concurrently": "^8.2.1"
  }
}
